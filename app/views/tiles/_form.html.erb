<%= form_for(@tile) do |f| %>
  <% if @tile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tile.errors.count, "error") %> prohibited this tile from being saved:</h2>

      <ul>
      <% @tile.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tile_id %><br>
    <%= f.text_field :tile_id %>
  </div>
  <div class="field">
    <%= f.label :tile_value %><br>
    <%= f.text_field :tile_value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
